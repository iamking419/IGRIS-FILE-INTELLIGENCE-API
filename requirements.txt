# IGRIS AI Backend — Dependencies
# =================================

# Core framework
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.10.0

# Google Gemini AI (only needed for live mode, mock works without it)
google-genai>=1.0.0

# Document extraction
pdfminer.six>=20240727
python-docx>=1.1.0

# Optional: Enhanced image support (Pillow for metadata extraction if needed)
pillow>=11.0.0

# Optional: Archive support enhancements
 py7zr>=0.22.0      # For 7z support
 rarfile>=4.2       # For RAR support